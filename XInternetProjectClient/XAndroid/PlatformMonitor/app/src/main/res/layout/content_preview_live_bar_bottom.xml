<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:id="@+id/preview_bottom_layout" 
    android:background="@android:color/transparent">

    <LinearLayout
        android:id="@+id/bottomLinearLayout"
        android:layout_width="match_parent"
        android:layout_height="42dp"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="false"
        android:layout_alignParentRight="true"
        android:layout_alignParentBottom="false"
        android:gravity="right">

        <ImageButton
            android:id="@+id/btnBarCapLive"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="@android:color/transparent"
            android:src="@drawable/zz_player_bar_cap_selector"
            android:scaleType="centerCrop"
            android:layout_marginRight="22dp"
            android:visibility="gone"/>

        <ImageButton
            android:id="@+id/btnBarRecLive"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="@android:color/transparent"
            android:src="@drawable/zz_record"
            android:scaleType="fitCenter"
            android:layout_marginRight="22dp"
            android:visibility="gone"/>


        <ImageButton
            android:id="@+id/btnBarTalkLive"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginLeft="22dp"
            android:background="@android:color/transparent"
            android:src="@drawable/zz_talkback" 
            android:visibility="gone"/>

        <ImageButton
            android:id="@+id/btnBarSoundLive"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="@android:color/transparent"
            android:src="@drawable/zz_sound"
            android:scaleType="fitCenter"
            android:layout_marginRight="22dp" />

    </LinearLayout>

</RelativeLayout>